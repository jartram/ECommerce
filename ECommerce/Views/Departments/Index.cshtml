@model IEnumerable<ECommerce.Models.Department>

@{
    ViewBag.Title = "Deparments";
}

<h2>Deparments</h2>

<p>
    @*First new, navigation parameters, second, paramentes html*@
    @Html.ActionLink("Create New", "Create", new  {  }, new { @class = "btn btn-primary" } )
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DeparmentId }, new { @class = "btn btn-warning" }) |
            @Html.ActionLink("Details", "Details", new { id=item.DeparmentId }, new { @class = "btn btn-info" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DeparmentId }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
